sudo docker exec -u 0 -it logstash "/bin/sh"


frame.time_epoch
ip.src
ip.dst_host
tcp.dstport
udp.dstport

Leggo dst_port, classifico servizio. Leggo hostname se esiste, classifico se riesco tipo di sito.
  
